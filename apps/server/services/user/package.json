{
  "name": "@pedaling/user-microservice",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsnd --respawn --transpile-only src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "prisma db push",
    "db:seed": "tsnd ./prisma/seed.ts"
  }
}
