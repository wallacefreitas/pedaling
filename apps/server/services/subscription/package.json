{
  "name": "@pedaling/pedal-subscription",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsnd --respawn --transpile-only src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:seed": "tsnd ./prisma/seed.ts"
  }
}
