syntax = "proto3";

service PedalingService {
  rpc ListAllUsers (Empty) returns (UserList) {}
  rpc CreateUser (User) returns (User) {}
}

message User {
  optional string id = 1;
  string name = 2;
  string email = 3;
}

message UserList {
  repeated User users = 1;
}

message Empty {}
